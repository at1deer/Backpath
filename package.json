{
  "name": "backpath",
  "version": "0.6.1",
  "description": "Minimal Backpath CLI prototype",
  "private": true,
  "bin": {
    "backpath": "bin/backpath.js"
  },
  "files": [
    "bin/",
    "lib/",
    "schemas/",
    "docs/",
    "examples/",
    "README.md",
    "BUILD_ORDER.md"
  ],
  "scripts": {
    "test": "node --test",
    "check": "node bin/backpath.js validate examples/manifests/distinction-collision.approval.json"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "ajv": "^8.20.0"
  }
}
